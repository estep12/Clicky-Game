import React from "react";

const LostCards = (props) => (
    <div className="card">
        <div className="images">
            <img src={props.img} alt=""/>
        </div>

    </div>
)

export default LostCards